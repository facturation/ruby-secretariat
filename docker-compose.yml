volumes:
  bundle:

services:
  test:
    build:
      context: .
    command: bash -c 'cd /app && bundle && bundle exec rake'
    volumes:
      - .:/app
      - "bundle:/bundle"
    environment:
      BUNDLE_PATH: /bundle
